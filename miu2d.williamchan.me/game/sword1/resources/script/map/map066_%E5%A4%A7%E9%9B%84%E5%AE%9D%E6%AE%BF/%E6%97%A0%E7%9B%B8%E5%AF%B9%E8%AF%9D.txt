// 与无相的对话

	If ($Event == 370) @Event370;
	If ($Event == 380) @Event370;
	If ($Event == 390) @Event370;
	If ($Event == 400) @Event370;
	Return;
	
@Event370:
	Say("无相：少林有七十二绝技，谁要是学透其中的一种就可天下无敌。只是出家人的武功再好，也只不过用来强身健体，多活几年罢了。", 73);
	Return;
	
